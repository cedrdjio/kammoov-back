{
    "version": 2,
    "builds": [
      {
        "src": "api/**/*.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/api/.*", "dest": "api/index.ts" }
    ],
    "env": {
      "PRISMA_CLIENT_ENGINE_TYPE": "binary"
    }
  }
  